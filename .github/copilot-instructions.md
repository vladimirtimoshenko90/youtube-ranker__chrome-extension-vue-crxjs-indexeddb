# Copilot Instructions

## Vue components

- Always `<script setup lang="ts">` — no Options API
- Always `<style scoped lang="scss">` — nest selectors to mirror the DOM structure
- Use `lucide-vue-next` for icons — import named icons, render as `<IconName :size="N" />`; no inline SVGs
